{% extends 'base.html.twig' %}

{% block title %}Moderator{% endblock %}

{% block body %}
    <h1>Moderator</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ moderator.id }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ moderator.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_moderator_index') }}">back to list</a>

    <a href="{{ path('app_moderator_edit', {'id': moderator.id}) }}">edit</a>

    {{ include('moderator/_delete_form.html.twig') }}
{% endblock %}
